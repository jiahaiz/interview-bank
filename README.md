# Interview Project

In-interview coding exercise. Candidate gets the repo from git.

---

## In-Interview Exercise

| Item | Purpose |
|------|--------|
| **interview-billing-scaffold/** | Q2: Reliable Event Ingestion (Python). Candidate designs first, then implements POST /events, GET /users/{id}/stats, and EventService so all tests pass. Tests fail until implemented. AI allowed. |

**Candidate:** Clone repo, open `interview-billing-scaffold/`, read its README, design → implement → run `pytest -v`.
